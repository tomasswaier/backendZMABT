$kind: http-example
request:
  url: "{{baseUrl}}/auth/signup"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "username": "string",
        "email": "51HfIEdaL@TcKT.xp",
        "password": "string",
        "passwordConfirmation": "string"
      }
response:
  statusCode: 400
  statusText: Bad Request
  body:
    type: text
    content: ""
order: 2000
